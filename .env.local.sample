# Local overrides — loaded after .env on your Mac (gitignored).
# Docker Compose only reads .env; containers reach Postgres via the service name.

APP_DB_HOST=localhost

# Uncomment if you run Prefect on your Mac (serve-flows, init-prefect, CLI):
# PREFECT_API_URL=http://localhost:4200/api
